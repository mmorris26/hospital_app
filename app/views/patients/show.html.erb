<h1> <%= "#{@patient.first_name}  #{@patient.last_name}"%>'s Page</h1>

<h3> <%= @patient.born_on %></h3>
<h5> <%= @patient.diagnosis %></h5>

<h3> <%= link_to "Edit Patient", edit_patient_path(@patient) %></h3>

<h3> <%= link_to "Delete Patient", patient_path(@patient),
                data: { 
                        turbo_method: :delete, 
                        turbo_confirm: "Are you sure you want to delete #{@patient.first_name}"
                        } %></h3>

<h3> <%= link_to "Back to Patients?", patients_path%></h3>
